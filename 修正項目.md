# 修正項目

`js/` 配下 22 ファイル（common 6 / sites 15 / loader 1）と `manifest_site.json` /
`manifest_folder.json` / `site_ids.json`、および git 管理外テンプレート内のローダー実体を
通読して洗い出した課題の一覧。

ビルド・テストが無く、ローカル検証は構文チェックのみ。`git push` した瞬間がデプロイになるため、
実機に出す前に机上で潰せるものをここにまとめる。

---

## 前提：スクリプトは画面遷移のたびに再取得・再評価される

多くの項目がここに帰着するため最初に書く。

`記録テーブル_テンプレート.json` の `Scripts[0].Body` のローダーは

```js
$.getScript("https://cdn.jsdelivr.net/gh/.../js/" + name)
```

で全スクリプトを読む。Pleasanter のサイト設定 Scripts は **pjax 画面遷移のたびに再実行される**。

1. **画面遷移のたびに manifest + 対象 JS 全本が再ダウンロードされる。**
   `$.getScript` は jQuery 内部で `cache: false` 固定 → `?_=<timestamp>` が付き、
   ブラウザキャッシュを毎回バイパスする。一覧⇄編集を 10 往復すれば 10×(1+7) ≒ 80 リクエスト。
2. **全スクリプトが遷移のたびに再評価される。**
   `window.__xxxBound` ガードはこのためにある。ガードが無いファイル／
   トップレベル `const` を持つファイルは、この時点で壊れる。

---

## 完了済み

### ✅ A-1 / D-3. `loader_folders.js` を manifest 方式に統一

**対応日**: 2026-09-18

旧 `loader_folders.js` は `port_setting.js` / `icon.js` の 2 本を github.io からハードコードで読んでいた。
`manifest_folder.json` に追記してもテナント側だけ追随せず、「manifest の編集だけで完結」という
設計原則が破れていた唯一の箇所だった。また取得元が違うため（manifest 経由 = jsdelivr、
loader_folders = github.io）、両経路が効く画面では同じ 2 本が別 URL から二重ロードされていた。

対応内容:

- `manifest_folder.json` の `Js.All` を読む方式に変更。取得元は jsdelivr `@js_fix` に統一
- `$.getScript` をやめ、`<script>` 要素の自前挿入に変更（`cache:false` によるキャッシュバスト回避）。
  `s.async = false` でマニフェストの記載順を保持
- `script[src=...]` による重複チェックを追加
- `window.runTenantScripts` / `window.reapplyFolderScripts` を export。
  後者は [loader_tenant.js:70](readonly_tenant/loader_tenant.js:70) が呼ぶのに未定義で、
  **2 回目以降の `pjax:complete` で再適用パスが死んでいた**（`fbca712` のリグレッション）
- CSS は読まない。`tenant.css` がトークン定義を含む自己完結版のため、
  `Css.All`（`custom_folders.css`）を重ねるとトークンが二重定義になる
- CLAUDE.md の該当箇所（配信元の説明・ブランチ切替時の書き換え対象・`readonly_tenant/` セクション）を更新

検証（`node` 未導入のためローカルサーバ＋実ブラウザで実施）:

| 確認項目 | 結果 |
|---|---|
| 構文・export | パース成功、両関数とも function |
| manifest 取得 | jsdelivr `@js_fix` から 200 |
| 読み込み順 | `port_setting.js` → `icon.js`（記載順どおり） |
| 再適用の冪等性 | 3 回呼んでも script タグは 2 本のまま |
| キャッシュバスト | リクエスト 3 本のみ、`?_=` なし |
| 実スクリプト実行 | 実 jQuery 下でエラー・警告 0 |

### ✅ A-2 / A-3 / A-4. `calendar_swap_on_drop.js` のデータ破壊・競合・未ガード

**対応日**: 2026-09-24

- A-2: 衝突相手への送信を `{ Id, [dateFieldKey] }` のみに変更（`baseFields` を空にした）。
  ドラッグ元の `Ver`・タイトル等が衝突相手に書き込まれる問題を解消
- A-3: `$.when(A, B)` の並行送信を `A.then(() => B)` による直列送信に変更
- A-4: `draggedRow` / `originalDateValue` が取得できない場合は入れ替えを中止し、
  `console.warn` を出して通常の単独更新にフォールバックするよう変更

### ✅ A-5. Pleasanter API の POST ボディが空

**対応日**: 2026-09-24

- [calendar_swap_on_drop.js](js/sites/calendar_swap_on_drop.js) と
  [holiday_setting.js](js/sites/holiday_setting.js) の `/api/items/{id}/get` POST に
  `data: JSON.stringify({ ApiVersion: 1.1 })` を追加。ボディ空のままでは
  衝突検出（swap側）が一度も成功していなかった可能性がある
- ⚠️ 実機での動作確認は未実施（F 節の実機確認 1 は引き続き要対応）

### ✅ A-6. トップレベル `const` による再評価時 SyntaxError

**対応日**: 2026-09-24

- `holiday_setting.js` / `comment_color.js` を IIFE + `'use strict'` で包み、
  トップレベル `const` の再宣言による `SyntaxError` を解消
- ついでに A-13 の関連バグも修正: `holiday_setting.js` の `removeClass("holiday-100 ...")` ハードコードを
  `removeClass(function(){...})` による `holiday-` 前置クラスの総なめに変更。
  `comment_color.js` の `map[name]` を `hasOwnProperty` 経由に変更し、debounce タイマーに `clearTimeout` を追加、
  デッドコードの `id` 変数を削除

### ✅ A-7. `icon.js` / `port_setting.js` — 多重登録ガードが無い

**対応日**: 2026-09-24

- `icon.js` に `window.__iconBound` ガードを追加。`pjax:complete` ハンドラと body 全体の
  MutationObserver が画面遷移のたびに積み重なる問題を解消
- 同じ問題があった `port_setting.js` 前半 IIFE にも `window.__portSettingBound` ガードを追加
- ついでに E 節の指摘も修正: `icon.js` の `querySelectorAll` を try/catch で囲み、
  不正セレクタで以降のアイコンが全滅する問題を解消。カスタムアイコンマップの型検証も追加

### ✅ A-8. `lock_save_during_upload.js` × `prevent_double_submit.js` の相互打ち消し

**対応日**: 2026-09-24

- `lock_save_during_upload.js`: アップロード終了時、ボタンに `is-submitting`（送信中）クラスが
  付いている場合は `disabled` を解除しないよう変更。アップロード中は従来通り無条件で無効化する
- `prevent_double_submit.js`: 安全解除タイマー発火時、ボタンに `is-upload-locked`
  （アップロードロック中）クラスが付いている場合は `disabled` を解除しないよう変更
  （アップロード終了時に `lock_save_during_upload.js` 側が改めて解除する）
- ついでに A-13 の関連バグも修正: 安全解除タイマーに `clearTimeout` が無く連続クリックで
  早期解除されうる問題を、タイマーIDを保持して解消

### ✅ A-9. `get_record_data.js` — Promise が永久ハング

**対応日**: 2026-09-24

- `getRecord()` に `fail` コールバックを追加し、API エラー・認証切れ・ネットワーク断時にも
  reject するよう変更
- 10 秒のタイムアウトを追加し、`fail` すら呼ばれないケースでもハングしないようにした
- `data.Response.Data` が空配列の場合に `undefined` を返していた経路もガードし、
  reject するよう変更
- ついでに E 節の指摘も修正: `getClassFromRecord` のデバッグ用 `console.log` 残骸を削除、
  末尾に改行を追加

### ✅ A-10 / A-11. `readonly.js` / `port_setting.js` — `disabled` によるフォーム値欠落・セレクタ生成

**対応日**: 2026-09-24

- A-10: `readonly.js` のフォーム要素無効化を `disabled` から、text系は `readOnly` 属性、
  select/checkbox/radio（readonly非対応）は `pointer-events:none` + `tabindex="-1"` に変更。
  同じ問題があった `port_setting.js` の `#Encoding`（select）も同様に変更し、
  Encoding パラメータがサーバに届かなくなる問題を解消
- A-11: `data-source="#${this.id}"` → `$(selector)` の生成をやめ、`data-source-id="${id}"` を
  `document.getElementById()` で参照する方式に変更。id が空文字や、ドットを含む場合の
  誤動作・例外を解消
- ついでに A-13 の関連バグも修正: date-field の Shadow DOM 監視で `setTimeout` の
  ハンドル未保持によるタイマーリークを解消

### ✅ A-12. `manifest_site.json` の `Js.New` は到達不能

**対応日**: 2026-09-24

- `__pleasanterScreenType()` が `"New"` を返すことは無く、`Js.New` は死にカテゴリだったため削除。
  `scroll_to_error.js` は `Edit` に同じファイルが既に登録されているため実害は無い
- CLAUDE.md の「Js のみ New も別枠」の誤記は D-7 対応時に既に解消済み

### ✅ A-13. その他の小バグ（一部）

**対応日**: 2026-09-24

- `empty_grid_message.js`: 0件判定を `tbody tr`（全 `<tr>`）から `tbody > tr.grid-row` に変更し、
  グループ化表示の見出し行混入による誤判定を解消。既存メッセージの検出も `.next()`（直後の兄弟）限定から
  `.nextAll().first()` に変更し、無限増殖しうる問題を解消
- `select_all_shortcut.js`: `preventDefault()` を「チェック済みか」判定の前で呼んでいたのをやめ、
  常にラベルの `click` をトリガーするよう変更。2回目のCtrl+Aが死にキーになる問題を解消
  （チェック済みなら選択解除、未チェックなら選択にトグルする）
- `attachment.js`: プレビューオーバーレイの `close()` をモジュールスコープに公開し、Escapeハンドラから
  再利用（`is-loading` の消し忘れを解消）。`href` 未取得時に `load` が発火せず `is-loading` が
  永久に残る問題に8秒のタイムアウトを追加。ホバーサムネイルの内側 `setTimeout(...,200)` の
  ハンドル未保持（離脱直後の再ホバーで表示中のものが消える）も解消
- `tooltip.js`: 同種の内側 `setTimeout(...,200)` のハンドル未保持を解消
- 残り3件（`message_output.js`のXSS要実機確認、`calendar_from_to_filter.js`の多言語対応要否、
  `search_shortcut.js`のキーボード配列対応要否）は製品判断が必要なため保留

### ✅ B-1 / D-5. `$.getScript` のキャッシュバスト停止 ＋ 画面種別判定の pjax 対応

**対応日**: 2026-09-24

- B-1: テンプレート（`記録テーブル_テンプレート.json` / `フォルダ_テンプレート.json`）の
  `window.__pleasanterLoadScript` を `$.getScript`（`cache:false`固定でキャッシュバストされる）から、
  `<script src>` 要素の自前挿入＋`script[src=...]`の重複ガードに変更（`js/loader_folders.js`と同じ形）。
  記録テーブル側は画面種別（screenType）の再判定＋manifest再取得も `pjax:complete` のたびに実行し、
  新たに必要になったファイルだけを追加読み込みするようにした
- D-5: B-1 により各スクリプトファイルは画面遷移のたびに再取得・再評価されなくなったため、
  画面種別判定を評価時1回しか行っていなかった以下を pjax 対応に変更:
  - `holiday_setting.js`: カレンダー画面判定〜APIフェッチの初期化を `tryInitHolidaySetting()` として
    `pjax:complete` のたびに再チェックし、初回成功時のみ実行する方式に変更
  - `readonly.js`: 読取専用判定・UI調整一式を `run()` として `pjax:complete` のたびに再実行するよう変更
    （ついでに E 節の「IIFE化されていない」指摘も解消）
  - `calendar_from_to_filter.js`: 既存の `pjax:complete` 再適用に加え、設定ダイアログの遅延生成を
    捉える MutationObserver（50ms debounce）を追加

### ✅ B-2 / C-1. body 全体 MutationObserver の集約

**対応日**: 2026-09-24

- `js/common/dom_watcher.js` を新設。`window.__pleasanterWatch(fn, { delay, guard, attributes, attributeFilter })`
  で購読し、`document.body`/`subtree:true` の MutationObserver を1個に集約
- 各購読者の実行前後で `disconnect() → 実行 → observe()` を回す
  （holiday_setting.js:103-114 の自己発火防止パターンを共通側に1回だけ実装）
- `manifest_site.json` / `manifest_folder.json` の `Js.All` 先頭に追加し、
  `port_setting.js` / `icon.js` より必ず先に読まれるようにした
- 移行した個別Observer: `icon.js`（25ms）／`port_setting.js`（25ms、インポート監視分のみ。
  エクスポート進捗オーバーレイのダイアログ限定observerは対象外）／`tooltip.js`（50ms）／
  `zebra_stripe.js`（50ms）／`attachment.js`（50ms、ダウンロードボタン付与分のみ。
  長押し並べ替え・サムネイルの document イベントリスナーは対象外）／
  `empty_grid_message.js`（50ms）／`lock_save_during_upload.js`（50ms、`attributes:true`）／
  `comment_color.js`（50ms、`attributes:true`）
- `readonly.js` の date-field Shadow DOM 監視は `MutationRecord`（`addedNodes`）そのものが
  必要なため対象外（`dom_watcher.js` はコールバックに変更内容を渡さない設計）。
  従来通り単独の MutationObserver のまま、多重登録のみガード
- `holiday_setting.js` は毎回無条件で `.holiday-name` を削除・再挿入する描画のため、
  集約すると全購読者を巻き込んだ再発火ループを起こしうる。専用の
  disconnect→描画→observe 実装（良かった点として明記済み）のまま維持し、対象外とした

### ✅ C-2. `js/common/utils.js` の新設（escapeHtml / once / ensureSingleton の共通化）

**対応日**: 2026-09-24

- `js/common/utils.js` を新設。`window.escapeHtml` / `window.once(name, fn)` /
  `window.ensureSingleton(id, html)` を提供する。manifestの`Js.All`先頭
  （`dom_watcher.js`より前）に追加し、全画面で最初に読まれるようにした
- `escapeHtml`の重複を解消: `holiday_setting.js` / `readonly.js` の個別実装を削除し、
  共通版を参照するよう変更
- `window.__xxxBound`による多重登録防止ガードを`once()`に統一:
  `icon.js` / `port_setting.js`（インポート監視・エクスポート進捗の2箇所）/
  `empty_grid_message.js` / `lock_save_during_upload.js` / `zebra_stripe.js` /
  `comment_color.js` / `attachment.js`（プレビュー・並べ替え・サムネイルの3箇所）/
  `readonly.js`（date-field監視）/ `tooltip.js`（E節で指摘されていた`$('#list-tooltip').length`
  によるDOM存在チェック方式も統一）
- 「#idが無ければ生成」パターンを`ensureSingleton()`に統一:
  `port_setting.js`のエクスポート進捗オーバーレイ、`attachment.js`のプレビューオーバーレイ・
  サムネイルポップアップ、`tooltip.js`の`#list-tooltip`

### ✅ C-9. CLAUDE.md に「スクリプトは遷移のたびに再評価される」を明記

**対応日**: 2026-09-24

- CLAUDE.md のコーディング規約節に、IIFE + `'use strict'` が必須である理由
  （Scripts はpjax遷移のたびに再実行され、トップレベル `const`/`let`/関数宣言があると
  2回目以降の評価で `SyntaxError` が起きファイル全体が停止する）を追記。
  A-6 で実際に踏んだ `holiday_setting.js` / `comment_color.js` を実例として明記
- 「多重登録は `window.__xxxBound` で防ぐ」の記述を、C-2 で統一した
  `window.once(name, fn)` を使う旨に更新

### ✅ D-1. CDN ブランチ名のハードコードを 1 箇所に寄せる

**対応日**: 2026-09-24

- 両テンプレートの Htmls 側インライン `<script>`（`__pleasanterEnv` を定義している箇所）で
  `window.__pleasanterCdnBase = "https://cdn.jsdelivr.net/gh/tomofuji-natsumi/pleasanter-resources@js_fix"`
  を1回定義し、同じブロック内の CSS ローダー（manifest fetch・`<link>` 生成）もこれを参照するよう変更
- JS 側（[js/loader_site.js](js/loader_site.js) / [js/loader_folders.js](js/loader_folders.js) /
  [holiday_setting.js](js/sites/holiday_setting.js)）と両テンプレートの Scripts スタブは
  `window.__pleasanterCdnBase || "<フォールバック値>"` の形で参照するよう変更
  （`window.__pleasanterEnv` と同じ流儀）。Htmls と Scripts の評価順序が保証されないため、
  フォールバック値は残してある
- `readonly_tenant/loader_tenant.js` 経由（github.io、テンプレート未経由）で
  `loader_folders.js` が読み込まれる場合は `window.__pleasanterCdnBase` が未定義のため、
  フォールバック値がそのまま使われる（従来と同じ動作）
- ⚠️ `main` マージ時の書き換え箇所は、両テンプレートの `__pleasanterCdnBase` 定義と、
  各JSファイルのフォールバック値の計5箇所に減った（従来はJS側3箇所＋テンプレート2箇所が個別）

### ✅ D-4. `zebra_stripe.js` を `Js.All` へ

**対応日**: 2026-09-24

- `manifest_site.json` で Edit / Calendar / Index の3カテゴリに重複登録されていた `zebra_stripe.js` を
  `All` に集約。ファイル自体が `.grid` の有無で自己ガードしているため、
  TimeSeries / ImageLib 画面で読み込まれても実害はない

### ✅ D-6. CLAUDE.md の記述と実態のズレ

**対応日**: 2026-09-24

- 「Js のみ New も別枠」の誤記は D-7 対応時に既に解消済みだったことを確認
- テンプレートの「CRLF・BOM 無し」の記述を「実測で全行 CRLF、BOM 無し」に修正
- `Styles[]` の「空枠」という記述を、実際にコード（コメント欄背景色・更新履歴非表示・
  読取専用文言非表示など）が入っていて、`#E7F1FF` / `#AFCBFF` が色指定ルールの例外である旨に修正
- 読み込みアーキテクチャの節は D-7 対応で既に実態に合わせて更新済みだったことを確認

### ✅ D-7. ローダー実体を git 管理下に出す

**対応日**: 2026-09-24

- アーキテクチャの心臓部（画面種別判定・manifest fetch・スクリプト挿入）を、記録テーブル側は
  新規の [js/loader_site.js](js/loader_site.js) として git 管理下に切り出した。
  テンプレート（`記録テーブル_テンプレート.json`）の `Scripts[0].Body` は
  `<script src="…/js/loader_site.js">` を挿入するだけの薄いスタブに置き換え
- フォルダ側（`フォルダ_テンプレート.json`）は同じ目的の処理を独自に持つ必要がなく、
  既にgit管理下にある [js/loader_folders.js](js/loader_folders.js)（`window.reapplyFolderScripts`）を
  そのまま読み込んで呼ぶ形に変更し、ロジックの二重管理を解消
- CLAUDE.md の該当箇所（読み込みアーキテクチャ図・ローダー実体の説明・ブランチ書き換え対象）を更新

---

## A. 修正（バグ）

### ★ 最優先

（対応済み。「完了済み」節を参照）

### 高

### 中

### A-13. その他の小バグ

| # | 箇所 | 内容 |
|---|---|---|
| [ ] | [message_output.js:12,23,34](js/sites/message_output.js:12) | `Text: message` を未エスケープで `$p.setMessage` へ。HTML レンダリングされるなら XSS。**要実機確認** |
| [ ] | [calendar_from_to_filter.js:15,23](js/sites/calendar_from_to_filter.js:15) | `/^日付[A-Z]$/` 等の日本語ラベル決め打ち。多言語環境で無効、逆に「日付B」という正規名の列を誤削除。**多言語対応要否の判断が必要なため保留** |
| [ ] | [search_shortcut.js:26](js/sites/search_shortcut.js:26) | `e.shiftKey` で return。AZERTY / ドイツ語配列では `/` に Shift が必須なため機能しない。**対象キーボード配列の判断が必要なため保留** |

---

## B. 高速化

### ✅ B-2. body 全体 MutationObserver の集約 ★効果大

（対応済み。「完了済み」節を参照）

### ✅ B-3. `tooltip.js:9-26` — レイアウトスラッシング ★ホットスポット

**対応日**: 2026-09-24

`updateClamped()`を2パス構成に変更。1パス目は`scrollHeight`/`clientHeight`/`scrollWidth`/`clientWidth`の
読み取りのみを行い、対象を配列に振り分ける。2パス目でまとめて`addClass`/`removeClass`を適用し、
読み取りと書き込みの交互実行による強制同期レイアウトを解消した。

### ✅ B-4. `lock_save_during_upload.js:41-46` — 監視範囲

**対応日**: 2026-09-24

- `isUploading()`を全件`filter(':visible')`から最初の可視要素で打ち切る方式に変更（無駄な走査を削減）
- 属性監視（`attributes:true`）をdocument.body全体からやめ、`.control-attachments`コンテナ
  だけを対象にした専用MutationObserverに変更。進捗バーの`style`更新等、添付欄と無関係な
  body全体の変更を拾わないようにした
- コンテナが後から追加されるケース（繰り返しテーブル項目の追加等）に備え、
  `js/common/dom_watcher.js`の軽量な（`attributes:false`の）購読で新規コンテナの出現を検知し、
  専用Observerの監視対象に追加する

### ✅ B-5. `readonly.js:35-45` — 50ms ポーリング

**対応日**: 2026-09-24

`watchDateField()`の冒頭で`df.shadowRoot`を即時チェックし、既に生成済みなら
`setInterval`を一切発生させず処理するように変更。多くの場合date-fieldは
（Pleasanter本体のスクリプトが先に読み込まれているため）呼び出し時点で既にshadowRootを
持っており、実質的にポーリングは発生しなくなる。
`attachShadow()`自体はMutationObserverで観測できない（DOM変更として記録されない）ため、
間に合わなかった稀なケースのみ既存の50msポーリング＋1秒タイムアウトにフォールバックする
（MutationObserverへの完全な置換は、Web標準の制約上できないと判断した）。

### ✅ C-3. `js/common/api.js` の新設（Pleasanter API の薄いラッパ）

**対応日**: 2026-09-24

`POST /api/items/{siteId}/get`が`calendar_swap_on_drop.js`と`holiday_setting.js`に
別々の実装で散在し、ApiVersion付与・ページング対応（B-6）をそれぞれ個別に持っていた。
`window.pleasanterApi.getRecords(siteId, options)` / `extractRows(res)` に集約し、両ファイルから利用するよう変更。
`holiday_setting.js`側は元々failハンドラが無く、失敗時に無言で放置されていた欠落も解消した。

### ✅ C-4. `js/common/overlay.js` の新設（オーバーレイ生成 + Escape + 外側クリック）

**対応日**: 2026-09-24

`window.createOverlay(id, html, options)`を新設。「#idが無ければ生成→is-visibleで表示切替→
Escape/外側クリック/指定セレクタのクリックで閉じる」という同型のコードを持っていた
`attachment.js`（プレビューオーバーレイ、独自の`onClose`でiframeリセット等を追加処理として渡す）・
`image_lightbox.js`・`port_setting.js`（エクスポート進捗オーバーレイ、Escape/外側クリックは無効化）を移行。
`scroll_to_top.js`のボタン生成は（オーバーレイではないため）`window.ensureSingleton`（C-2）に統一のみ行った。

### ✅ C-5. `js/common/hover_popup.js` の新設（ホバー追従ポップアップ）

**対応日**: 2026-09-24

`window.createHoverPopup(id, html, itemSelector, options)`を新設。マウス追従表示・遅延非表示の
実装が`attachment.js`（サムネイル）と`tooltip.js`（省略テキスト）にバグまで含めてほぼ同一の形で
重複していたため、1箇所に集約。表示要否・内容反映は`onEnter($popup, $item)`コールバックに委ねる形にした。

### ✅ C-6. `js/common/shortcut.js` の新設（キーボードショートカットの入力欄ガード）

**対応日**: 2026-09-24

`window.registerShortcut({ key, ctrlOrMeta, allowShift, handler })`を新設。「入力欄でのタイピングを
妨げない」判定と修飾キー判定が`holiday_setting.js`（矢印キー・Tキー）/ `search_shortcut.js`（"/"）/
`select_all_shortcut.js`（Ctrl+A）に重複していたため、1箇所に集約。`select_all_shortcut.js`は
元々Shiftとの併用を許可していた（他2つは不許可）ため、`allowShift`オプションで挙動の違いを吸収した。

### ✅ C-7. ローカル検証コマンドの一本化

**対応日**: 2026-09-24

`tools/check_js_syntax.py`を新設。CLAUDE.mdが検証コマンドとして案内していた`node --check`は、
この環境に`node`が入っておらず実行できなかった。文字列・コメント・正規表現リテラルの中身を
読み飛ばした上で`{ } ( ) [ ] `の対応だけを見る簡易チェッカー（本物のパーサではないが、
このリポジトリで実際に起きる「括弧の閉じ忘れ」系のミスは検出できる）をPythonのみで実装し、
CLAUDE.md・本ファイルの検証手順を`python tools/check_js_syntax.py`に置き換えた。
わざと壊したファイルで誤検出・見逃しが無いことを確認済み。

### ✅ C-8. `manifest_*.json` のカテゴリ名タイポ検出

**対応日**: 2026-09-24

`tools/check_manifest.py`を新設。`manifest_*.json`の`Js`/`Css`のキー（画面種別）にタイポがあっても
JSON構文としては正しく通ってしまい、該当画面でスクリプトが永久に読み込まれないまま気づかれにくい
（実例: 過去の`Js.New` — A-12）。`jsonschema`パッケージが無い環境のため、標準ライブラリのみで
キーが固定enum（`All`/`Edit`/`Calendar`/`Index`/`TimeSeries`/`ImageLib`）に含まれるか・参照パスが
実在するか・同一カテゴリ内で重複していないかを検証するスクリプトを自作した。
Cssセクションの参照パスは`js/`ではなく`css/`を基点に解決される点に注意（当初これを誤って実装し、
既存2ファイルの全Css記載を誤検出していたのを修正済み）。過去の`Js.New`問題を模したテストで
検出できることを確認済み。

### ✅ B-6. `calendar_swap_on_drop.js` — 全件取得のページング未考慮

**対応日**: 2026-09-24

衝突検出用の`/api/items/{id}/get`リクエストに`View: { PageSize: 100000 }`を追加し、
既定上限（200件）超のサイトでも全件取得できるようにした。`location.reload()`は
FullCalendarに再取得を指示する手段が無いため、コメントの方針どおり現状維持とした。
⚠️ PageSizeの上限はPleasanter側設定に依存するため実機確認が必要。

### ✅ B-7. `attachment.js:381` — ホバーで原寸画像をダウンロード

**対応日**: 2026-09-24

サムネイルホバー時のURLに`?thumbnail=1`を付与するよう変更（`image_lightbox.js`が
認識するのと同じパラメータ）。ホバーのたびに原寸画像をフルダウンロードしていた問題を解消。

### ✅ B-8. `attachment.js` — イベント負荷

**対応日**: 2026-09-24

- `document.elementFromPoint()`を伴う`handleDragMove()`の呼び出しをrequestAnimationFrameで
  1フレーム1回に間引いた（`scheduleDragMove()`/`cancelScheduledDragMove()`）
- `mousemove`/`touchmove`（`passive:false`）の登録を常時ではなく、長押し開始（`onPressStart`）〜
  ドラッグ終了・キャンセル（`clearPress()`/`finishDragging()`が両方アイドルになった時点）の
  間だけ動的に付け外しするよう変更。ページ全体のスクロール最適化が常時無効化される問題を解消

### ✅ B-9. `get_record_data.js` — メモ化

**対応日**: 2026-09-24

`getRecord()`にrecordId単位の短期キャッシュ（TTL 5秒、Promiseそのものをキャッシュ）を追加。
同時に呼ばれた`getXById`系は同一のin-flightリクエストに相乗りし、少し後の呼び出しも
キャッシュを再利用するため、同一レコードから複数項目を取得しても実際のAPI往復は1回になる。
失敗時はキャッシュに残さず次回呼び出しで再取得する。

---

## C. 追加

| # | 提案 | 理由 |
|---|---|---|
| [x] C-1 | **`js/common/dom_watcher.js`**（B-2） | observer 集約。最も効果が大きい新規ファイル（対応済み） |
| [x] C-2 | **`js/common/utils.js`** — `escapeHtml` / `once(name, fn)` / `ensureSingleton(id, html)` | `escapeHtml` が [holiday_setting.js:42](js/sites/holiday_setting.js:42) と [readonly.js:80](js/sites/readonly.js:80) に重複。`window.__xxxBound` ガードは 13 種類が手書き（対応済み） |
| [x] C-3 | **`js/common/api.js`** — Pleasanter API の薄いラッパ（対応済み） | `POST /api/items/{id}/get` が散在していた。ApiVersion・ページング・エラー warn を1箇所に集約 |
| [x] C-4 | **`js/common/overlay.js`** — オーバーレイ生成 + Escape + 外側クリック（対応済み） | attachment.js / image_lightbox.js / port_setting.js が同型のオーバーレイ生成コードを持っていた |
| [x] C-5 | **`js/common/hover_popup.js`** — ホバー追従ポップアップ（対応済み） | attachment.js と tooltip.js が**バグまで含めて同一**の実装を持っていた |
| [x] C-6 | **`js/common/shortcut.js`** — キーボードショートカットの入力欄ガード（対応済み） | holiday_setting.js / search_shortcut.js / select_all_shortcut.js に重複していた |
| [x] C-7 | ローカル検証コマンドの一本化（対応済み） | `node` が未導入のため CLAUDE.md の `node --check` が実行できなかった。`tools/check_js_syntax.py`（Pythonのみで動く括弧対応チェッカー）に置き換えた |
| [x] C-8 | `manifest_*.json` のカテゴリ名タイポ検出（対応済み） | カテゴリ名のタイポ（`New` 問題と同種）を静的に検出できる。`jsonschema`パッケージが無いため`tools/check_manifest.py`を自作 |
| [x] C-9 | CLAUDE.md に「**スクリプトは遷移のたびに再評価される**」を明記（対応済み） | 「多重登録は `window.__xxxBound` で防ぐ」だけでは、トップレベル `const` が禁止である理由が伝わらない |

---

## D. 変更（設計・運用）

- [ ] **D-2. `site_ids.json` の `prod` が未設定**

  ```json
  "prod": { "holidayCalendarSiteId": null }
  ```

  現在テンプレートは `__pleasanterEnv = "dev"` が有効。
  **本番切替と同時に実 SiteId を入れないと祝日表示が無言で無効化される**
  （[holiday_setting.js:132-134](js/sites/holiday_setting.js:132) の warn のみ）。
  → 本番 SiteId を確定させて埋める。リリース手順書にも残す。

---

## E. 改善（規約準拠・可読性）

- [x] **`console.warn` の徹底**。`js/common` の **6 ファイル全てに try/catch も console 出力も無い**。
  特に [icon.js:157](js/common/icon.js:157)（不正セレクタ → `querySelectorAll` が throw →
  **以降のアイコンが全滅**）と [icon.js:192](js/common/icon.js:192)（カスタムマップの型検証なし）は実害が大きい。
  `mergeCustomDefs` は [L189](js/common/icon.js:189) で `merged = true` を先に立てるため、
  壊れた定義でも再試行されない。（対応済み: 実害のあった icon.js は A-7 で try/catch・型検証済み。
  他の js/common ファイル（lock_save_during_upload.js / prevent_double_submit.js / scroll_to_top.js /
  overlay.js / hover_popup.js / shortcut.js / utils.js / port_setting.js）は JSON.parse や
  外部由来セレクタ等の「投げうる」処理を持たず、DOM操作のみのため追加の try/catch は不要と判断）
- [x] **ガードの書き方が 3 通り混在**。
  [empty_grid_message.js:32-45](js/common/empty_grid_message.js:32)（ガード内で登録・ガード外で初回実行）、
  [scroll_to_top.js:12-13](js/common/scroll_to_top.js:12)（全体 early return → **2 回目以降は初回実行もされない**）、
  [zebra_stripe.js:19-21](js/sites/zebra_stripe.js:19)（実行が先・登録が後 = 正しい）。
  → `zebra_stripe.js` の形に統一する。（対応済み: `scroll_to_top.js` を「実行が先→`once()`内でイベント登録」の形に修正。
  `empty_grid_message.js` / `zebra_stripe.js` は既にこの形だったため変更なし）
- [x] **IIFE 化されていないファイル**: `comment_color.js` / `get_record_data.js` / `holiday_setting.js` /
  `message_output.js` / `readonly.js` / `tooltip.js`。A-6 の前提としても必要。（対応済み: 全ファイル末尾が `})();` でIIFE化確認済み）
- [x] [tooltip.js:32](js/sites/tooltip.js:32) のガードが `$('#list-tooltip').length`（DOM 存在チェック）で、
  規約の `window.__xxxBound` と流儀が違う。要素が消えると二重登録される。（対応済み: C-2で`window.once('tooltip', ...)`に統一済み）
- [x] [port_setting.js:96](js/common/port_setting.js:96) の `.off("click")` が名前空間なしで、
  他コードのハンドラも消す（同ファイル [L101](js/common/port_setting.js:101) は `change.import` と正しく書けている）。
  （対応済み: `click.import` 名前空間に統一）
- [x] [get_record_data.js:44](js/sites/get_record_data.js:44) に `console.log(record)` のデバッグ残骸。
  （対応済み: A-9で既に削除済みだったことを確認）
- [x] [get_record_data.js:43-231](js/sites/get_record_data.js:43) の 12 関数はハッシュ名だけが違う 6 組の双子。
  **約 190 行 → 20 行のファクトリ**に圧縮できる。
  同様に [message_output.js:11-36](js/sites/message_output.js:11) の 3 関数は Css 文字列違いのみ。
  （対応済み: `get_record_data.js` を `RECORD_FIELD_TYPES` 配列 + `forEach` ファクトリに、
  `message_output.js` を共通 `outLog(css, message)` に圧縮。関数名・エラーメッセージ文言は従来通り）
- [x] [attachment.js:42-50](js/sites/attachment.js:42) の `getFileName` / `getShowUrl` はデッドコード
  （L131-132 でインライン実装されている）。（対応済み: 未使用の重複定義を削除）
- [x] [comment_color.js:25-26](js/sites/comment_color.js:25) の `id` は算出後に一度も使われないデッドコード。
  （対応済み: 既に削除済みだったことを確認）
- [x] 末尾改行なし: `js/sites/get_record_data.js`, `js/sites/message_output.js`。（対応済み: 末尾改行1つに統一）
- [x] [loader_tenant.js:80](readonly_tenant/loader_tenant.js:80) の `window.startIconObserverForIcons` が未定義のまま。
  `icon.js` は `window` に何も export せず自走するため、呼び出し側だけが残っている。
  `typeof ... === 'function'` でガードされているので実害は無いが、消すかどうか判断が要る。
  （対応済み: 呼び出し箇所ごと削除）

---

## F. 要実機確認

机上では判定できず、Pleasanter 実機での確認が必要なもの。

1. [ ] `/api/items/{id}/get` を**ボディ空**で POST して 200 が返るか（A-5）
2. [ ] `$p.setMessage` の `Text` が HTML としてレンダリングされるか（A-13 / `message_output.js`）
3. [ ] `#CreateCommand` / `#Encoding` / `#Import` が `<button>` / `<input>` のどれか（A-10）
4. [ ] `#DeleteCommand` に Pleasanter 標準の `data-confirm` が付いているか
   — [confirm_delete_title.js:40-43](js/sites/confirm_delete_title.js:40) が `btn.click()` で再発火させるため、
   併存すると**確認ダイアログが 2 回出る**可能性。ファイル冒頭が自ら ⚠️ 未検証と明記している
5. [ ] [confirm_delete_title.js:30-32](js/sites/confirm_delete_title.js:30) の `stopImmediatePropagation()` —
   document の capture フェーズで呼んでいるため、後から登録された全 capture リスナ
   （例: `attachment.js:331`）を巻き込んで停止させていないか
6. [ ] [calendar_swap_on_drop.js:84](js/sites/calendar_swap_on_drop.js:84) が `options.beforeSend` を無条件で上書き。
   Pleasanter が設定した CSRF ヘッダ等の処理を破壊していないか
7. [ ] [calendar_swap_on_drop.js:36-37](js/sites/calendar_swap_on_drop.js:36) —
   値側には `+`→空白置換があるがキー側には無い（不整合）。また `decodeURIComponent` は不正な `%` 列で throw し、
   `ajaxPrefilter` 内なので**ページ上の全 ajax が巻き添えで壊れる**
8. [ ] [calendar_swap_on_drop.js:46-50](js/sites/calendar_swap_on_drop.js:46) の `toDateKey` のタイムゾーン依存。
   `new Date(value)` は ISO の `Z` 付き文字列でローカル日付が 1 日ずれ、衝突判定を誤る
9. [ ] [attachment.js:218-222](js/sites/attachment.js:218) — `order.indexOf(guid)` が未知の GUID に `-1` を返し、
   JSON 側にのみ残る要素が silently 先頭へ移動する
10. [ ] [attachment.js:253,331-334](js/sites/attachment.js:253) — `suppressNextClick` が「実際に動かしたか」を見ていない。
    長押しして動かさず離すとプレビューが開かず、逆にフラグが残ると次のクリックを 1 回握り潰す
11. [ ] [holiday_setting.js:54,56,59-60](js/sites/holiday_setting.js:54) — 列名 `ClassA`/`ClassD`/`ClassE`/`DateA` と
    マジックナンバー `"200"` のハードコード。SiteId は外出しできているのに、列構成という同種の結合が残っている
12. [ ] [holiday_setting.js:142-150](js/sites/holiday_setting.js:142) — `sessionStorage` の中身を検証せず代入。
    文字列 `"null"` がキャッシュされていると `holidayMap = null` となり L79 で毎回 TypeError。
    キャッシュキーにバージョン/TTL が無く、マスタ更新がタブを閉じるまで反映されない（SWR で緩和）
13. [ ] [restore_scroll_position.js:45-47](js/sites/restore_scroll_position.js:45) — 単発 rAF のみ。
    グリッドが非同期描画される場合、文書高が足りず `scrollTo` が暗黙にクランプされて復元位置が失われる（断続再現型）
14. [ ] [scroll_to_top.js:37](js/common/scroll_to_top.js:37) — `window.scrollY` 前提。
    内側スクロールコンテナのレイアウトではボタンが一生出ない（scroll はバブリングしない）
15. [ ] [lock_save_during_upload.js:19](js/common/lock_save_during_upload.js:19) — `[id$=".status"]` は
    `.status` で終わる無関係な id も拾う。誤検知すると保存不能になる
16. [ ] [lock_save_during_upload.js:30](js/common/lock_save_during_upload.js:30) — `attr('title','')` は属性を削除せず
    空文字で上書きするため、標準やテーマが付けた title が恒久的に失われる。`removeAttr('title')` が正しい

---

## 良かった点（変えないほうがよいもの）

- [holiday_setting.js:103-114](js/sites/holiday_setting.js:103) — `disconnect() → 描画 → observe()` を
  **正しく実装している唯一の箇所**。共通化の際はこれを雛形にする。
- [restore_scroll_position.js](js/sites/restore_scroll_position.js) — rAF スロットル、
  sessionStorage の例外ガード、[L31](js/sites/restore_scroll_position.js:31) で
  `scroll_to_error.js` との競合を明示的に回避。**最も品質が高い**。
- [zebra_stripe.js:19-21](js/sites/zebra_stripe.js:19) — 「毎回実行」と「1 回だけ登録」の分離が正しい。
- [image_lightbox.js](js/sites/image_lightbox.js) — 委譲イベントのみで pjax に自然対応。常駐 observer なし。
- **XSS は全体的に良好**。`innerHTML` への変数連結は 0 件。
  [holiday_setting.js:87](js/sites/holiday_setting.js:87) と [readonly.js:151](js/sites/readonly.js:151) は
  `escapeHtml` を正しい順序で適用している。懸念は `message_output.js` の 1 箇所のみ。
- manifest の記載パスは **13 JS + 5 CSS すべて実在**、未登録ファイルはすべて意図的な予備。
  JSON は BOM 無し・LF・2 スペース・末尾改行ありで完全に統一。

---

## 推奨する着手順

| 段 | 内容 | 根拠 |
|---|---|---|
| ~~0~~ | ~~A-1 / D-3（loader_folders.js の manifest 化）~~ | ✅ 完了 |
| ~~1~~ | ~~A-2 / A-3 / A-4（calendar_swap_on_drop のデータ破壊）~~ | ✅ 完了 |
| ~~2~~ | ~~A-6（IIFE 化）＋ A-7（icon.js ガード）＋ A-8 / A-9 / A-10 / A-11~~ | ✅ 完了 |
| ~~3~~ | ~~D-7（ローダーを git 管理下へ）→ B-1（キャッシュ）＋ D-5（pjax 対応）~~ | ✅ 完了 |
| ~~4~~ | ~~A-5（POST ボディ空）＋ A-12（`Js.New` 死にカテゴリ）~~ | ✅ 完了（A-5 は実機確認のみ未了、F 節に残す） |
| ~~5~~ | ~~C-1 / B-2（observer集約）→ C-2（共通ユーティリティ）→ B-3 / B-4 / B-5~~ | ✅ 完了 |
| ~~6~~ | ~~A-13 の主要項目（empty_grid_message.js 等）~~ | ✅ 完了（残り3件は製品判断待ちで保留） |
| 7 | E 系の個別修正、D-2（設定）、B-6〜B-9（効果小） | D-1/D-4/D-6 完了 |
| 8 | F（実機確認）| 上記と並行して随時 |

---

## 検証方法

ローカルでできるのは構文チェックのみ。**`node` は未導入の環境を前提とする**（C-7、対応済み）。

```bash
python tools/check_js_syntax.py
```

`js/` 配下の全 `.js` を対象に、文字列・コメント・正規表現リテラルを読み飛ばした上で
`{ } ( ) [ ]` の対応だけを見る簡易チェッカー（本物のパーサではない）。個別ファイルだけ見たい場合は
`python tools/check_js_syntax.py js/sites/holiday_setting.js` のようにパスを渡す。

```bash
python -c "import json,glob;[json.load(open(f,encoding='utf-8')) for f in glob.glob('js/*.json')];print('ok')"
```

`manifest_*.json` のカテゴリ名タイポ検出（C-8、対応済み）:

```bash
python tools/check_manifest.py
```

`Js`/`Css` のキーが画面種別の固定enum（`All`/`Edit`/`Calendar`/`Index`/`TimeSeries`/`ImageLib`）から
外れていないか・参照パスが実在するか・同一カテゴリ内で重複していないかを確認する。
JSON構文としては正しく通るため通常の構文チェックでは検出できないタイポ（実例: 過去の `Js.New` — A-12）
を機械的に検出できる。

JS の挙動を実際に確かめたい場合は、`scratchpad` に検証用 HTML を置いてローカルサーバで開く方法が使える
（`js/loader_folders.js` の検証で実施。manifest 取得・読み込み順・冪等性・キャッシュバスト有無まで確認できた）。

```bash
python -m http.server 8731 --bind 127.0.0.1
```

効果測定:

- pjax 遷移を 3 回行い、DevTools Network で同じ JS が何回落ちてくるか（B-1）
- `performance.getEntriesByType('resource')` と Performance パネルで
  MutationObserver 集約前後の scripting 時間を比較（B-2）
